#include<stdio.h>
int main(){
    int a=10,b=20;
    int sum=a+b;//"=" is an assign operator
    int mod=a%b;
    int pro=a*b;
    int sub=b-a;
    float div=a*1.0/b;//vagfol float pete hole 2ta jinish must. first one is 1.0 die gun dite hobe and the second one is assign variable ta must float nite hobe
    printf("%d %f %d %d %d",sum,div,sub,pro,mod);
    return 0;
}